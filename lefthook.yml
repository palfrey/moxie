pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "*.{cjs,js}"
      run: ./pnpm prettier --write {staged_files}
    scad:
      glob: "*.scad"
      run: bash scad_format.sh {staged_files}